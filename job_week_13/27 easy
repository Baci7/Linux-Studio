int removeElement(int* nums, int numsSize, int val)
{
    int i,t = 0;
    for(i=0;i<numsSize;i++)
    {
        if(nums[i]!=val)
        {
            nums[t] = nums[i];
            t++;
            
        }
    }
    return t;
}
